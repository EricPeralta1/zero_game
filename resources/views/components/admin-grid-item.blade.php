<div class="user-admin">
    <span>{{ $nombre }}</span>
    <span>{{ $correo }}</span>
    <span><button class="update-btn" data-id="{{ $id }}">Modificar</button></span>
    <span><button class="remove-btn" data-id="{{ $id }}">Eliminar</button></span>
</div>
